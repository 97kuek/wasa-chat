# WASA Chat TODO

更新日: 2026-08-11

完了済みの経緯と実測値は[docs/08-測定記録.md](docs/08-測定記録.md)、評価の運用は
[docs/06-評価とフィードバック.md](docs/06-評価とフィードバック.md)を参照する。

**この文書は「いま精度を確定させるために何をするか」を書く場所である。**
引き継ぎ、監視、混雑、承認の更新など、機能が増えなくても消えない課題は
[docs/09-運用課題.md](docs/09-運用課題.md)が正本。条件を満たしてやると決めたものだけ、
向こうからこちらへ移す。

## 優先度A: 本文中の出典（`[n]`）— 測定済み。残りは1回だけ（M39）

- 35問で測定した（M39）。**範囲外の番号は222個中0個。** 番号だけを書かせる方式は
  意図どおり働いた。ページ選択・出典・回答の出し方に退行なし
- **測定器の欠陥を1件直した（5回目）。** `strip_citations` がJudgeへ渡す前に
  資料番号を落としておらず、正しい文を裏付け無しと判定していた
- 残っている作業
  - **直したあとで全問を測り直す。** 4問だけの測り直しでは、回答の作り直しによる
    ばらつきと切り分けられていない。きれいなFaithfulnessの数字はここで出す
  - ⚠️ **2026-09-12に回答プロンプトへ表と図（mermaid）の出力規則を足した（M49）。**
    測り直しは必ずこの変更の後に行う。それ以前の「回答の出し方」の数字は、
    いまの出力を説明しない
  - `--repeats 3` で範囲を見る。1回の実行の上振れを改善と読まない
  - q24 だけ番号が1つも付かなかった理由を、回答本文で確認する
- 本番Go（`backend/internal/pipeline`）と測定用Python（`rag/pipeline.py`）の
  両方へ同じ規則と番号の振り方が入っている。**片方だけ直さないこと**

## 優先度A: 精度を確定する

- API接続は復旧済み（M23）
  - 原因はプリペイド残高ではなく、プロジェクトの請求階層が「無料トライアル」だったこと
  - 課金を紐づけない`wasa-chat`プロジェクトを作り、無料枠のキーへ差し替えた
  - **本番GCPプロジェクトから課金を外さないこと。** Cloud RunとFirestoreに必要
  - 本番Secret Managerのキーは**別のキーで、正常に動いている**（M29で確認）。更新不要
  - ただし本番とローカルでキーが違う。どちらのプロジェクトのキーかは未確認
- 測定は完了した（M23）
  - q32・q33の実モデル回答: ページ的中2/2、出典2/2。TR797の誤答は解消を確認
  - 33問全体: ページ的中29/31、出典29/33、回答の出し方26/33、Faithfulness 32/33
  - 回答モード比較: **thinking levelを上げてもページ選択は改善しない**（fast 28/30・
    standard 29/30・deep 28/30）。deepはfastの4.1倍の時間
  - 汎用と区分アシスタント: 3問では**差を検出できない**（どちらも9/9）
- 会話・リンク検索の確認は完了した（M24）
  - 追質問「それについて解説して」が尾翼設計を引き継ぐことを実モデルで確認した
  - リンク検索は「リンクを教えて」では動くが**「どこにありますか」で発火しなかった**ため、
    `どこ|ありか`を追加し、決定的候補の合流順を`実在タイトル → 型番 → リンク`へ変更した
  - 合流順を変えずに「どこ」だけ足すと、q26が退行する（19/33 → 18/33）
  - 出所を指定した質問の除外はGoテストで固定済み
- 残っている確認
  - 出典の欠落を減らす（`global`と`false_premise`に集中している）
    - q18・q24・q30で出典が付かなかった。q26は「記載がない」ため出典不要で正しい
  - 回答生成側のthinking level比較と、Context Precisionの測定
  - **`GEMINI_DEEP_MODEL`へ強いモデルを割り当てる判断は引き続き見送る**
  - リンク検索が人物ページ（岡村圭吾）を候補に出す。実害が出たら弾く規則を検討する
- LLM Judgeを人手評価で校正する
  - 20問を人手とJudgeの両方で評価する
  - Cohen's κとfalse-accept率を算出する
  - 一致度が低い間はFaithfulnessの自動値を確定指標にしない
  - **誤検知の実例を1件つかんだ**（M23）。q33で資料の要約を「裏付けなし」と判定していた

## 優先度A: 利用者フィードバックを改善へ戻す

- 本番でテスト報告を1件送り、Firestoreへの保存と手元への書き出しを確認する
- `go run ./cmd/export-feedback`でJSONL／CSVへ定期的に書き出す
  - JSONLはスクリプト処理、CSVは人手の分類に使う
  - 生データには非公開Wikiの内容が含まれるため、リポジトリへコミットしない
  - CSVの確認状態、問題層、修正内容、回帰テストIDを記録する
- 一定期間、回答評価と画面全体の報告を集める
- 質問数に対する評価率と、評価済み回答に対する👎率を分母付きで出す
- 👎を理由別に人手確認する
  - 内容が違う、出典が違う: Faithfulnessと根拠を確認する
  - 必要な情報がない: ページ・節選択を再現する
  - 回答が遅い: 保存された段階別所要時間と照合する
- 実データで確認できた不具合だけゴールデンデータへ追加する
- 保存期間と削除方針は決定済み（2026-08-09に人間が判断）
  - フィードバックは1年。新しい報告の保存時に、期限切れをサーバーが自動削除する
  - チャット履歴は従来どおり新しい30件のみ
  - `web/public/support.html`のプライバシーポリシーに同じ内容を明記した

## 優先度B: 所要時間を分析する

- 実利用データが集まった後、次のp50 / p95を測る
  - ページ選択
  - 節選択
  - 回答生成
  - 合計
- 回答モードとアシスタント別に比較する
- 実測分布を見る前に「何秒以上を遅い」と決めない
- API待機、再試行、モデル生成のどこが外れ値を作ったか確認する

## 優先度B: 検索精度を上げる

- 決着した（M24）。Evidence Recall@5 17/31の失敗14問のうち
  - 10問は実在タイトルまたは型番候補でページを保持する
  - 残る4問を実モデルで3回ずつ測ったところ、**q07・q20・q30は3/3で解けていた**
  - **言い換え対応の検索層は追加しない。** 「作業場→作業場情報」「鳥人間コンテスト→鳥コ
    ン」は、目次を見て選ぶ経路では問題にならなかった
  - **q18（false_premise）だけが0/3。** ただし回答側では前提を正しく訂正できている。
    検索の課題として扱う前に、正解ページの定義が妥当かを人手で見直す
- 数字を含まない略語は、実際の誤答が出たものだけ対応する
- 新しい検索層は、前段の実測で不足が確認できた場合だけ追加する
- 変更後はページ単位ではなく根拠チャンク単位で再測定する

## 優先度B: 画像の添付を育てる（2026-08-10に実装）

- 汎用の添付機能として入れた。仕組みは docs/04 と docs/03 にある
- 残っている測定
  - **画像の大きさとトークン数の関係**（384 / 768 / 1536px）。日次上限で中断した
  - ネタツイの出来。ゴールデンデータを作れないので👍／👎と「投稿に使われたか」だけ
- 次に足すなら
  - PDF（Geminiは直接読めるが、ページ数と費用が読めないので測ってから）
  - 複数枚。**実際に必要になった例が出てから**にする
- 「広報（ネタ）」は参照範囲が公式サイトのみ。非公開Wikiが公開ツイートへ
  混ざる経路を構造的に塞いでいる。**ここを緩めないこと**

## 優先度B: 画像の必要性を測る（索引側）

- 測定は完了した（M21）。67枚中55枚が本文の57箇所から参照され、うち図が唯一の
  情報源なのは15箇所だった
- 先に画像キャプション抽出の欠陥を直した。日本語の表示パラメータ
  （`サムネイル` `中央` `なし`）を除外していなかったため、本文の図57件中31件が
  無意味な文字列になっていた。検索精度に退行はない
- **画像処理はまだ足さない。** ゴールデンデータ33問の正解チャンクに図は0件で、
  実利用の誤答としても報告が無い
- 足すときの対象は67枚全部ではなく、次の11枚に限る
  - 数式画像（`Expresssion` 系9枚 + `Math_1.png`）
  - `EE-SX460-P1 ピン配置.png`
- 着手の条件は、「必要な情報がない」の👎に図が絡む例が実利用で出たとき

## 優先度C: UXと運用を仕上げる

- キーボードだけの操作はコード上で確認し、不足を直した（M22）
  - 改善、お知らせ、プロフィールの切り替え: Tab・Enter・Escapeで操作できる
  - フィードバック分類、補足、送信、閉じる: すべてボタンと入力欄で到達できる
  - アシスタントの並べ替えと作成: 矢印・Home・End・Enter・Escapeに対応済み
  - 閉じた後にフォーカスが`body`へ落ちていた4箇所を、開いた元へ戻すよう直した
  - 実装していない`role="menu"`をやめ、`role="group"`と操作対象名にした
- スクリーンリーダーの実機確認は**やらないと決めた**（2026-08-10に人間が判断）
  - 部内向けの道具で、いまのところ必要としている利用者がいないため
  - **コード側の対応は入れたまま残す。** 消すと入れ直すのが大変で、
    入れてあること自体に費用はかからない
  - 手順は[docs/05-フロントエンド.md](docs/05-フロントエンド.md)に残してある
- フィードバックが100件で探しにくくなった場合だけ、期間・理由フィルターと出力を追加する
- 初回JavaScriptが再び500kBへ近づいた場合だけ、追加のコード分割対象を測る
  - 2026-08-11時点で250.83kB（gzip 78.88kB）。管理画面29.44kB（gzip 8.08kB）は遅延読込済み
- デプロイ後にPCとスマホでログイン、質問、追質問、評価、履歴同期をスモークテストする
  - `/support.html`がログイン前後の両方から開けることも確認する
  - `GEMINI_RPD_LIMIT=500`を設定し、主管理者・共同管理者だけが`/admin`を開けることを確認する
  - 更新確認専用WikiアカウントをSecret Managerへ登録し、管理画面からWiki・公式サイトの差分を確認する
  - 管理画面で質問本文を表示せず、質問後に個人回数とGemini送信試行回数が増えることを確認する
  - 「フライトシミュレータとはなんですか」「FEEってなに」が答えられるか
  - 削除したアシスタント6件が本番のFirestoreに残っている。管理者として画面から消す
- 画面が真っ白になる不具合は原因を特定して直した（M30）
  - 履歴の`sources`が`null`で返ることによる`.length`の例外だった
  - エラー境界を入れたので、次に落ちたときは理由が画面に出る
  - **同じ症状の報告が来たら、まず画面に出た例外の文言を聞くこと**
- 使い方・注意事項・プライバシーポリシーは`web/public/support.html`に作成済み
  - 実利用が始まったら、よくある質問を実際の問い合わせで差し替える
  - 保存期間を変える場合は、`support.html`とサーバーの削除処理を必ず同時に直す
- 更新確認は管理画面へ移行済み。Mac依存のlaunchdラッパーは本番確認後に削除した
  - 手動確認が必要な場合は`python check_updates.py`を直接実行できる
  - 取得・再構築・デプロイを自動化しない設計判断は維持する
- 索引はCloud Storageへ移した（M31）
  - 資料だけの更新は`sh tools/publish-index.sh`の1コマンド（約40秒）
  - コードを変えたときだけイメージのビルドとデプロイが要る
