# WASA Chatへの貢献について

このリポジトリを直すときの進め方をまとめたページです。

WASA Chatは、代が替わっても引き継げるように作っています。そのため
「動けばいい」ではなく、**次に触る人が理由を追えること**を重視しています。
少し細かいことも書いていますが、どれも過去に実際に困った経験から来ているものです。

まだリポジトリの中身を知らない場合は、先に
[docs/00-引き継ぎガイド.md](docs/00-引き継ぎガイド.md)を読んでください。

## まず環境を作る

外部依存は`requests`と`python-dotenv`の2つだけです。

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install requests python-dotenv
cp .env.example .env   # Wiki取得に使う通常アカウントの認証情報を記入する
```

バックエンドとフロントエンドは、それぞれ次で動きます。

```bash
cd backend && SESSION_SECRET=... DATA_DIR=../data go run .
cd web && npm install && npm run dev
```

## コミットする前に必ず実行するもの

3つとも数秒〜十数秒で終わります。ここで落ちるものを持っていくと、
あとで原因を探すのに何倍も時間がかかります。

```bash
cd backend
GOCACHE=/private/tmp/wasa-go-cache go test ./...

cd ../web
npm test
npm run build

cd ..
python -m py_compile rag/pipeline.py eval/response_mode_eval.py
```

`npm test`はNode標準のテストランナーで`web/scripts/*.test.mjs`を実行します。
新しい依存は入れていません（Viteが持っているesbuildだけを使います）。

いま守っているのは`markdown.tsx`です。ここは**描画が止まらなくなると
タブごと落ちる**場所で、例外と違ってエラー境界では受け止められません。
回答はSSEで少しずつ届くので、**途中のどの長さで切っても描画が終わること**を
1文字ずつ確かめています（2026-08-10に、表の1行目だけが届いた状態で
無限ループする不具合を見つけたため）。

検索や索引に関わる変更をした場合は、これも実行してください。

```bash
python eval/retrieval_eval.py
```

## 絶対にコミットしてはいけないもの

**引き継ぎWikiは非公開です。** コードは公開していますが、中身は公開していません。

- `.env`
- `dump/`（Wikiと公式サイトの取得データ）
- `data/`（索引と目次）
- `eval/golden.json`（設問に実データの引用を含む）

`.gitignore`には入っていますが、`git add -A`の前に`git status`で
これらが出ていないことを確認する癖をつけてください。

## 進め方の原則

### 測ってから足す

これがこのプロジェクトで一番大事な考え方です。

RAGの解説を読むと、検索層や再ランキングなど、足せるものがいくらでも出てきます。
しかし全部入れると**何が効いているのか分からなくなり、費用と待ち時間だけが増えます。**

新しい層や規則を足すのは、**前の段階の実測で「足りない」と確認できたときだけ**にしてください。

### 実際の誤答を根拠にする

プロンプトの規則や`facts.md`のカードを足すときは、
「こういうときに困りそう」ではなく「実際にこう間違えた」を根拠にします。

規則が増えるほど毎回の費用が上がり、モデルの読み落としも増えるためです。
**その文がどの失敗を防ぐのか説明できないなら、足さないでください。**

### 数字を出したら記録する

測定を行ったら、[docs/08-測定記録.md](docs/08-測定記録.md)へ次の4点セットで残します。

1. 日付 / 対象データの規模 / **再現コマンド**
2. 数字（表の形で）
3. **読み取れたこと** — その数字から何を判断したか
4. **判断を変えた点** — 設計方針のどこを書き換えたか

再現コマンドが無い数字は、あとから確かめようがありません。

### 数字が動いたら、まず測定器を疑う

精度が変わったと思ったら、実は評価スクリプト側の欠陥だった、ということが
このプロジェクトでは何度もありました（M2b、M6、M13）。

コードを直す前に、測り方が本番と同じかどうかを確認してください。

### 蒸し返さない判断がある

グラフRAG不採用、BLEU/ROUGE不採用、Refine不採用など、
一度検討して決めたものが[AGENTS.md](AGENTS.md)の表にまとまっています。

**再検討自体は歓迎です。** ただし「良さそうだから」ではなく、
[docs/01-設計方針.md](docs/01-設計方針.md)に書かれた根拠に反論する形でお願いします。

### 人間に確認してほしいこと

次の2つは、技術的に正しいかどうかとは別の判断が要ります。勝手に決めないでください。

- **認証方式**（Wikiのパスワードを扱う経路そのもの）
- **個人情報の扱い**（マスキングの範囲、保存期間、削除方針）

サークルのデータをどう扱うかという話なので、必ず先に相談してください。

## コードの書き方

### 日本語で書く

**ドキュメント、コメント、コミットメッセージはすべて日本語**です。

### コメントは「なぜ」を書く

「何をしているか」はコードを読めば分かります。書いてほしいのは**なぜそうしたか**です。

特に、実測で判断が変わった箇所には根拠になった数字を残してください。

```go
// 短時間待っても回復しないため、再試行せず太平洋時間0時まで止める。
```

```python
# 実データの画像パラメータを数えたところ、上位は日本語の別名だった
# （サムネイル39 / なし16 / 中央10）。除外していなかったため、
# 本文の［図: …］57件中31件（54.4%）が表示指定になっていた。
```

### 外部依存を増やさない

Python側は`requests`と`python-dotenv`だけです。
増やす場合は、それが無いと実現できないことを説明してください。

### 触るときに気をつける箇所

- **`ingest/dump_wiki.py`のリクエスト間隔（1秒）を縮めないでください。**
  相手はさくらインターネットのレンタルサーバです。取得に数分かかるのは意図的です
- **Ollamaを使うときは`OLLAMA_CONTEXT_LENGTH`を省略しないでください。**
  既定の4096では、目次（1〜1.5万トークン）が黙って切り捨てられます
- **プロンプトを変えるときは、本番Goと測定用Pythonの共通部分を両方直してください。**
  ただしアシスタント固有の規則は本番Goだけにあります

## コミットメッセージ

1行目に「種類: 要約」、空行、本文という形です。要約は命令形にしません。

```
種類: 一行の要約（日本語、命令形にしない）

何を変えたか。特に **なぜ** 変えたかを実測値とともに書く。
設計方針を変えた場合は、どのドキュメントのどこを更新したかも書く。
```

### 先頭に付ける種類

Conventional Commitsの標準的な語彙を使います。**この8つ以外は使いません。**

| 種類 | 使うとき | 例 |
|---|---|---|
| `feat:` | 利用者から見てできることが増えた | `feat: 質問に画像を1枚添えられるようにした` |
| `fix:` | 壊れていた動作を直した | `fix: 表のストリーミングで描画が無限ループする不具合を直した` |
| `docs:` | 文書だけの変更（README、docs/、AGENTS.md、facts.md） | `docs: スクリーンリーダーの実機確認はやらないと決めた` |
| `test:` | テスト、評価スクリプト、ゴールデンデータ、測定の実施 | `test: 評価器のテストと反復測定を足し、q17の失敗の型を特定した` |
| `refactor:` | 外から見える動作を変えずに構造を変えた | `refactor: 索引をCloud Storageへ移した` |
| `perf:` | 速度・サイズを改善した（**必ず前後の実測値を本文に書く**） | `perf: 回答描画を初期バンドルから分離` |
| `build:` | Dockerfile、依存関係、`.gcloudignore` など生成物に関わるもの | `build: Cloud Buildの安全な送信範囲を追加` |
| `chore:` | 上のどれでもない雑務 | `chore: ビルド生成物 tsbuildinfo をコミット対象から外す` |

種類の後ろは**半角コロン + 半角スペース**です。要約自体は日本語のままにします。

判断に迷ったときの目安です。

- **測定コミットは `test:`。** 数字を出す作業そのものは検証なので、
  結果を docs/08 に書いた場合でも `test:` にします。
  ただし**すでに出た数字の記録・整理だけ**なら `docs:` です
- **1つのコミットに種類が混ざるときは、そのコミットの主目的を選びます。**
  ただし混ざること自体が多いなら、まずコミットを分けられないか考えてください
- **スコープ（`fix(web):` のような括弧）は使いません。** 一人保守で、
  どこを触ったかは要約の日本語を読めば分かるためです

### マージコミットは対象外

`Merge pull request ...` のような、Gitが自動生成するマージコミットには種類を付けません。
Conventional Commitsの慣習に合わせています。

### 過去の履歴について

2026-08-11に、それ以前の89件へ後からこの規約を適用しました
（マージコミット2件を除く全件）。**このとき全コミットのSHAが変わっています。**
それより前のSHAを参照している手元のクローンやメモがあれば、貼り直してください。
書き換え前の履歴は `backup/before-msg-rewrite` ブランチにローカルで残してあります。

**`Co-Authored-By` などの共同作業者行は付けないでください。**
AIエージェントに作業させた場合も同じです。このリポジトリの著者は保守者本人であり、
コミット履歴を使った道具の一覧にはしません（2026-08-09に人間が判断）。

## 変更の種類ごとの確認先

どこを直すかによって、先に読んでおくべき文書が違います。

| 変えるもの | 先に読む文書 | 追加でやること |
|---|---|---|
| 画面 | [docs/05-フロントエンド.md](docs/05-フロントエンド.md) | `npm run build`と、狭い画面での重なり確認 |
| プロンプト | [docs/03-プロンプト.md](docs/03-プロンプト.md) | ゴールデンデータ全体で推論し、失敗を分類する |
| 検索・索引・マスキング | [docs/02-データ検索とルール.md](docs/02-データ検索とルール.md) | `eval/retrieval_eval.py`を実行し、docs/08へ記録 |
| ログイン・Cookie・Firestore | [docs/04-バックエンド.md](docs/04-バックエンド.md) | 人間の判断を先に得る |
| 本番設定 | [docs/07-デプロイと更新.md](docs/07-デプロイと更新.md) | `min-instances=0`と`max-instances=1`を維持する |
| 評価・フィードバック | [docs/06-評価とフィードバック.md](docs/06-評価とフィードバック.md) | — |
| 運用の体制・引き継ぎ・監視 | [docs/09-運用課題.md](docs/09-運用課題.md) | 片付いた項目は文書から消す |

文書を直したときは、それが**正本かどうか**を確認してください。
同じ話が2か所に書いてあると、片方だけ直したときに食い違いが生まれます。
どの話題がどの文書の担当かは、[docs/00-引き継ぎガイド.md](docs/00-引き継ぎガイド.md)にあります。

## AIエージェントに作業させる場合

Claude CodeなどのAIエージェントで作業する場合は、
[AGENTS.md](AGENTS.md)を読ませてください。確定済みの設計判断と、
実データで踏んだ落とし穴がまとまっています。

作業させたあとは、**必ず人間が差分を確認してください。**
特に、非公開データがコミットに含まれていないかは自分の目で見てください。

## 不具合を見つけたら

利用者として気づいたことは、アプリの画面から送るのがいちばん早いです。

- 回答が違っていた: その回答の👎から理由を選ぶ
- 画面がおかしい・使いにくい: 右上の「改善を送る」から報告する

開発者として直せる場合は、先に**再現できる失敗例**を用意してください。
検索の失敗ならゴールデンデータへ追加し、直ったことを数字で確認します。

## ライセンス

- コードは[MIT License](LICENSE)です
- **Wikiの内容と取得データはWASA鳥人間プロジェクトに帰属し、MIT Licenseの対象に含めません**
