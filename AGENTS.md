# AGENTS.md

- このリポジトリで作業するAIエージェント向けの指示
- 人間向けの概要は [README.md](README.md)を参照
- 引き継ぎ文書の入口は [docs/00-引き継ぎガイド.md](docs/00-引き継ぎガイド.md)を参照
- 設計判断の根拠は [docs/01-設計方針.md](docs/01-設計方針.md)を参照
- RAG精度の実測値は [docs/08-測定記録.md](docs/08-測定記録.md) を参照

## プロジェクト概要

- 早稲田大学宇宙航空研究会WASA 鳥人間プロジェクトの引き継ぎ資料Wiki（MediaWiki 1.37）に、
自然言語で質問できるチャットボット
- **本番運用中で、現在は精度の確定と運用改善フェーズ。**
- 2026-08-11時点での対象データ規模は以下のとおり
   - Wiki 114ページ
   - 公式サイト500ページ
   - フライトシミュレータガイド32ページ / 735,264字 / 1,012チャンク /
   - Wiki添付画像67枚（本文参照57箇所）

---

## セットアップと実行

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install requests python-dotenv
cp .env.example .env      # Wikiの認証情報を記入

python check_updates.py        # 必要なときだけ公開元との差分を確認
python rebuild.py              # 全資料の取得・索引作成・検索検査
python eval/retrieval_eval.py  # 検索精度を測定
```

`facts.md` は**人が保守する事実カード**で、`build_toc.py` が `data/toc.md` の先頭に
埋め込む（＝毎回のプロンプトのキャッシュ対象の先頭に載る）。資料を素直に読むと
間違える事柄だけを書く場所であり、**「実際に誤答した」という根拠を伴わないものを
足さないこと**。分量が増えるとキャッシュ費用と読み落としの両方が悪化する（M7）。

⚠️ **`facts.md` は一時置き場である。** 索引の外に知識を置く仕組みなので、
増やすほど「Wikiを直せば済むこと」を肩代わりする。カードを足したら
**Wikiの適切なページへ書くところまでを1組の作業**とし、書けたらカードは消す
（2026-08-09に人間が判断）。`facts.md` 冒頭に移行状況の表がある。

`dump_wiki.py` はさくらインターネットのレンタルサーバを叩くため、リクエスト間に1秒の間隔を入れている。
**この間隔を縮めないこと。** 全ページの取得に数分かかるのは意図的。

ローカルLLM（測定用）:

```bash
OLLAMA_FLASH_ATTENTION=1 OLLAMA_KV_CACHE_TYPE=q8_0 OLLAMA_CONTEXT_LENGTH=32768 \
  ollama serve
```

**コンテキスト長の指定を省略しないこと。** Ollamaの既定は4096で、目次だけで1〜1.5万トークンあるため
黙って切り捨てられる。

---

## 確定済みの設計判断（蒸し返さないこと）

根拠は [docs/01-設計方針.md](docs/01-設計方針.md) §1・§4・§5・§7 にある。異論があるなら、
その根拠に反論する形で行うこと。

| 論点 | 決定 |
|---|---|
| RAG方式 | ドキュメントRAG主軸 + 軽量メタデータ層。**グラフRAGは不採用**（117ページ規模で構築コストが便益を上回る） |
| 全域質問 | 階層的な事前要約。**Refineは不採用**（114件は長すぎ順序依存で劣化する） |
| RIG | **不採用**（照合先の権威DBが存在しない。ツール呼び出しループが上位互換） |
| 評価指標 | **BLEU/ROUGEは不採用**。日本語のn-gram一致は不安定で、改善に繋がらない指標は入れない |
| 評価の主軸 | LLM-as-a-Judge（Faithfulness最重視）+ ルールベースのガードレール + 少量の人手 |
| データ整形 | wikitext直処理（テンプレートが実質ゼロのため `action=parse` 不要） |
| 形態素解析 | **kagome に一本化**（Go側）。索引と検索で分かち書きが違うとBM25が壊れる |
| バックエンド | Go on Cloud Run（`min-instances=0` 固定） |
| フロントエンド | TypeScript + React (Vite) on Cloudflare Pages |
| データストア | 索引はJSON（現行約3.1MB）。利用者プロフィール（任意画像を含む）、利用回数、最大30件の履歴、共有アシスタント、利用者フィードバックをFirestoreへ保存 |
| 認証 | **Wikiのアカウント**（`action=clientlogin` に中継）+ 利用者名を載せた署名付きCookie |
| 更新反映 | **管理画面から差分確認、手動 rebuild・反映**。確認はCloud RunがWiki改訂番号と公式サイトのサイトマップだけを照合し、本文の再取得や自動反映はしない |
| アシスタント | **全員が作って共有できる。内容の審査はしない。** 設定画面は基本設定／参照範囲／指示／用語集のタブに分け、開いた直後は閲覧（編集は明示的に入る）。**用語集はアシスタントごと**に持つが、置けるのは語の言い換えだけで事実は置かない（出典が付かないため）。 代わりに構造で縛る（範囲は狭める指定のみでGo側が決定的に除外／出典は索引から組み立ててモデルに触らせない／規則はsystem側へ／作成者名を必ず出す／他人のものは読み取り専用で閲覧でき、変更は複製のみ）。⚠️ **保証の強さが違うものを混同しない**（コードの保証／緩和策／社会的な抑止）。詳細は [フロントエンド](docs/05-フロントエンド.md)、[プロンプト](docs/03-プロンプト.md)、[データ検索とルール](docs/02-データ検索とルール.md) |
| 管理者 | 共有アカウントは使わない。`ADMIN_USERS`は画面から外せない主管理者（復旧口）で、主管理者だけがWASA Chat利用者を共同管理者へ追加・解除できる。共同管理者は管理画面と運用操作を使えるが、権限の再付与はできない。実名の全利用者一覧・検索・ソートを出すが質問本文は出さない。アシスタント削除は作成者本人と両管理者だけ（2026-08-11に人間が判断） |

**段階的に作ること。** L0〜L4を初期版から全部入れない。各層は前段の実測で不足が
確認できたときにだけ追加する（§2のPhase分け）。

---

## 実データで踏んだ落とし穴

すべて実際に測って発見したもの。同じ轍を踏まないこと。

1. **短いページを検索対象から外さない。** 98字の「作業場移転履歴」に移転年・移転先・理由が
   完結して書かれていた。「短い＝価値がない」は誤り。除外は実質的に空（20字未満）のページのみ。

2. **評価はチャンク単位で行う。** ページ単位のRecallは実力を**17ポイント過大評価**する
   （Page Recall@5 = 72.4% に対し Evidence Recall@5 = 55.2%）。
   36,261字ある「駆動・フレーム班」がヒットしても、正解の節が取れているとは限らない。

3. **チャンクは最上位見出しをまたがない。** ただし小さいグループ同士の結合は許す。
   厳格にすると見出しだけの細切れチャンクが大量に出る（200字未満が122件になった）。

4. **パンくずはチャンク内全節の共通接頭辞から作る。** 先頭節から作ると嘘になる。

5. **PIIのマスキングはパース前に行う。** 後段だと `[[Mailto:...]]` が内部リンク扱いで
   `links` 配列に取り残される。

6. **班の分類では代のまとめページ・人物ページを先に弾く。** 本文に「空力設計」と書かれた
   名簿ページが空力班に混ざり、班フィルタが信用できなくなっていた。

7. **代（世代）の抽出は43%しか埋まらない。** かつ**不明の全てが抽出失敗ではない**。
   「ESP32のGPIOについて」のように代非依存の知識ページが相当数ある。
   LLM補完のタスクは「代を当てる」ではなく「代依存か非依存かを分類する」。

8. **最終更新日を情報の鮮度として使わない。** 911チャンク中155件（17%）で、本文が
   扱う年代が `last_edited` より2年以上古い。誤字直しでも更新日は今日になるため、
   原理的にずれる。内容の年代は本文中の西暦と代から別に拾う（`extract_era`）。
   なお `N代 = 鳥人間コンテスト(1984+N)年大会` で、この式は全代で例外なく一致する。

9. **ゴールデンデータのevidenceは必ず実データに照合する。** 出題時の想定と実データが
   食い違うことが複数あった（「新宿コズミックセンター」→ 正しくは「新宿コズミックスポーツセンター」
   かつ「使える」ではなく「断念した」など）。

---

## コーディング規約

- **ドキュメント・コメント・コミットメッセージはすべて日本語。**
- コメントは「何をしているか」ではなく**「なぜそうしたか」**を書く。特に、実測で判断が
  変わった箇所には根拠となる数字を残す。
- 外部依存は最小限に保つ。現状 `requests` と `python-dotenv` のみ。
- 数字を出す処理は、必ず**再現コマンドと結果を [docs/08-測定記録.md](docs/08-測定記録.md) に記録**する。

コミットメッセージの形式:

```
種類: 一行の要約（日本語、命令形にしない）

何を変えたか。特に **なぜ** 変えたかを実測値とともに書く。
設計方針を変えた場合は、どのドキュメントのどこを更新したかも書く。
```

先頭の「種類」は Conventional Commits の標準語彙で、**次の8つ以外は使わない**。

`feat:` 機能追加 / `fix:` 不具合修正 / `docs:` 文書のみ / `test:` テスト・評価・測定 /
`refactor:` 動作を変えない構造変更 / `perf:` 速度・サイズ改善 / `build:` ビルド・依存 /
`chore:` 雑務

⚠️ **測定を実施したコミットは `test:`。** 結果を docs/08 へ書いた場合でも `test:` にする。
すでに出た数字の記録・整理だけなら `docs:`。スコープ（`fix(web):`）は使わない。
マージコミットには種類を付けない。各種類の例と判断基準は
[CONTRIBUTING.md](CONTRIBUTING.md) の「コミットメッセージ」にある。

**`Co-Authored-By` などの共同作業者行は付けないこと。** このリポジトリの著者は保守者本人であり、
コミット履歴を道具の一覧にしない（2026-08-09に人間が判断）。

---

## 作業の進め方

**測ってから足す。** 推測で層を増やさない。

現在のフェーズと次の一手は [docs/01-設計方針.md](docs/01-設計方針.md) §9 にある。
新しい測定を行ったら [docs/08-測定記録.md](docs/08-測定記録.md) に、
「数字」「読み取れたこと」「判断を変えた点」の3点セットで記録すること。

未決の論点は §8 にある。勝手に決めず、判断が必要なら人間に確認する。
特に**認証方式**と**個人情報の扱い**はサークルのデータに関する判断であり、
技術的な最適解だけで決めてよい問題ではない。
