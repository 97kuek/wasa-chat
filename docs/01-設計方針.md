# WASA Chat 設計方針（v0.4）

> 引き継ぎ資料 01（全10文書）— 現在の構成を選んだ理由<br>
> [前: 00-引き継ぎガイド](00-引き継ぎガイド.md)｜[次: 02-データ検索とルール](02-データ検索とルール.md)

この文書は、WASA Chatが**なぜいまの形になっているのか**をまとめたものです。

作り方そのものではなく、「なぜこの方式を選び、なぜ別の方式を捨てたのか」を書いています。
引き継いだ人が同じ検討を最初からやり直さずに済むように、そして
一度捨てた案を蒸し返さずに済むように残しています。

ここに書かれた判断は、ほとんどが**実際に測った数字から決めたもの**です。
なので、異論があるときは「なんとなく良さそう」ではなく、その数字に反論する形でお願いします。

> 状態: **本番運用・精度の確定と運用改善フェーズ**。
> 過去のレビューと判断変更は§10に残している。

---

## 0. 前提となる実測値

設計の話をする前に、扱うデータがどれくらいの規模なのかを見ておきましょう。

**この数字がすべての判断の出発点になっています。**

例えば「ページ数が数万あるのか、
数百なのか」で、必要な仕組みはまったく変わります。WASA Chatの場合は後者でした。

現行索引は`build_index.py`と`build_toc.py`の実行結果です。
詳細な再現コマンドは[08-測定記録.md](08-測定記録.md)のM46にあります。

| 項目 | 実測 | 設計への含意 |
|---|---:|---|
| 全ページ | 646 | 単一JSONで扱える規模 |
| 内訳 | Wiki 114 / 公式サイト 500 / フライトシミュレータガイド 32 | `source`で出所を区別する |
| 索引本文 | 735,264字 / 1,012チャンク | ベクトルDBや分散DBは不要 |
| 目次 | 34,684字 | モデルに全体の見取り図を渡せる |

以下は、特に整形方針を決めたWiki単体の実測です。

| 項目 | 実測 | 設計への含意 |
|---|---|---|
| 本文ページ数 | 117（うちリダイレクト3、実記事114） | **極小**。分散DBもスケール設計も不要 |
| 総文字数 | 305,419字 | 現行マスキング・整形後 |
| 検索対象ページ | 104（実記事114のうち） | 20字未満だけチャンク化せず、短いページも検索対象に残す |
| チャンク数 | 358（中央値865字 / 最大2,431字） | 節の中央値は191字と細かすぎるため、階層マージ後の値 |
| 見出し数 | 865（H2:231 / H3:326 / H4:223 / H5:65 / H6:20） | 階層構造が明確。パンくずの精度が高い |
| 表（wikitable） | 39個 | 少ない。rowspan/colspanは未対応のまま許容 |
| テンプレート | **実質0**（DISPLAYTITLE/DEFAULTSORTのみ4件） | `action=parse`でのHTML化が不要。wikitext直で読める |
| 画像 | 67枚（PNG41・JPEG26）/ 本文参照57箇所 | キャプションが情報を持つ。必要性はM21で測定済み |
| 更新の生存性 | 117中68ページが2025〜2026年更新 | 生きたWiki。更新追随の設計が必須 |

### 公式サイト（v0.3で追加、`dump_site.py`）

途中で、Wikiだけでは足りないことが分かりました。

引き継ぎWikiは「作り方」にはとても詳しいのですが、**団体の成り立ちや歴代機体といった
対外的な説明がありません。** そのため「WASAとはどんなサークルか」「歴代の機体名と記録は」
といった質問に答えられませんでした。

そこで、一般公開されている https://wasa-birdman.com/ も取り込むことにしました。

| 項目 | 実測 | 備考 |
|---|---|---|
| 固定ページ | 31（本文80字未満の9件は除外） | 団体紹介・班紹介・機体一覧・年間の活動サイクル |
| 投稿（活動報告） | 479 | 2011〜2026年の試験飛行報告など。1件平均753字 |
| 取得時の総文字数 | 384,194字 | 短いページの除外と整形後、索引に入る本文は349,923字。**Wiki本文（300,425字）より多い** |
| 取得経路 | sitemap索引 → post/page-sitemap | WP REST APIは無効（`/wp-json/`が404）。`<loc>`はCDATA包み |
| ページ構成 | Elementor製。`entry-content`が無い | `data-elementor-type="wp-page"`を本文とみなす |
| robots.txt | `search=yes, ai-train=no, use=reference` | 出典リンク付きの参照は許可。学習には使わない |

4つの出所は`index.json`の`source`で区別しています
（`wiki` / `site` / `fee` / `drive`）。定義は `internal/index` の `Origins` にあり、
`build_index.py` とのずれはテストで見張っています。

⚠️ **共有ドライブ（`drive`）だけは既定で読みません。** 入力欄の「+」でオンに
したときだけ参照範囲へ入り、管理者の許可も要ります（docs/07 §5.6）。
目次も分けてあり、固定プレフィックスには含めません。

回答プロンプトでは「資料同士で食い違う場合は引き継ぎWikiを優先」と指示し、
画面の出典にもどちらから引いたかを表示しています。これは見た目の問題ではなく、
**その情報を部外に出してよいかどうかの判断に必要**だからです。

### ここから導かれる基本認識

以上の数字から、次の3つが言えます。

1. **データが想定以上にきれいです。** テンプレートが実質ゼロなので、wikitextを直接扱えます
2. **規模が小さいです。** ベクトルDBのインフラを組むより、全件をメモリに載せて
   総当たりする方が速く、正確で、安く済みます
3. **構造化のヒントが既にあります**（`空力設計(41st)`、`構造設計 38th`）。
   ただし、タイトルに代が入っているのは114ページ中26ページだけなので、
   機械抽出だけでは足りません（§6-2）

---

## 1. RAG方式の選定：ドキュメント / SQL / グラフ

RAGにはいくつかの方式があります。まずどれを土台にするかを決めました。

| 方式 | 本件での強み | 本件での弱み | 判定 |
|---|---|---|---|
| **ドキュメントRAG** | 本文の大半が非構造の散文・箇条書き。素直に効く | 全域質問に原理的に弱い | **主軸として採用** |
| **SQL RAG** | 属性クエリ・集計・並べ替えに強い。決定的で検証しやすい | 対象となる構造化DBが存在しない | **限定採用**（メタデータ層のみ） |
| **グラフRAG** | 「代 × 班 × 部品 × 人」の関係と相性は良い | 構築コストが規模に見合わない | **不採用** |

### 結論：ドキュメントRAG主軸 + 軽量メタデータ層

グラフRAGは魅力的に見えるので、見送った理由を書いておきます。

GraphRAGの主な価値は、**コミュニティ要約による全域質問への対応**であって、
グラフ構造そのものではありません。本件では、その価値の相当部分を
階層的な事前要約（§3）で安く得られます。117ページという規模では、
グラフを組むコストが便益を上回ると判断しました。

> ⚠️ v0.1では「階層的要約はGraphRAGと機能的に等価」と書いていましたが、
> **これは言い過ぎだったので撤回します。** 事前要約には
> 「要約時に落とした情報は永久に取り出せない」という弱点があり、
> グラフ探索による動的な関係発見とは等価ではありません。
> 全域質問への実用的な近似、という位置づけが正確です。

SQL RAGは、利点だけを限定採用しています。Text-to-SQLはやらず、
ページ属性をテーブルの形で持つだけです。

```
pages(id, title, aliases, 代, 代の根拠, 班, categories, 最終更新, revid, 文字数, is_stub, 要約)
chunks(id, page_id, パンくず, 本文, 文字数)
images(file, キャプション, 掲載ページ)
links(from_page, to_page)      -- 内部リンクから自動構築
```

これだけで「41代の資料一覧」「電装班の全ページ」「今年更新されたページ」は、
検索を経由せずに確実へ答えられます。

---

## 2. RAGの処理は3段に保つ

現在の回答経路は次の3段です。

```
質問
  → ① 目次からページを選ぶ
  → ② 選んだページから節を選ぶ
  → ③ 選んだ節だけで回答を作る
```

回答後、サーバーが索引上の資料と本文中の番号を結び付けます。
モデルに資料名やURLを作らせないため、存在しない出典を表示できません。

目次は引き継ぎ資料の見取り図です（共有ドライブの節は分けてあります）。ページ名・要約・出所を先に見せることで、
最初から全文を送らずに必要な範囲を選べます。型番、実在タイトル、リンクはコードで候補へ加え、
BM25は補助に使います。

この3段で不足が実測されるまでは、ベクトル検索、グラフRAG、質問ルーター、
クエリ書き換え、リランキングを追加しません。層を増やすと、費用と待ち時間だけでなく、
どこで精度が落ちたかを切り分ける手間も増えるためです。

### 形態素解析器はkagomeに一本化する（⚠️ BM25そのものはまだ無い）

**索引側（Python/SudachiPy）と検索側（Go/kagome）で分かち書きが食い違うと、BM25は壊れます。**
同じ文章が違う単語に分割されるので、一致するはずのものが一致しなくなります。
そのため、BM25を入れるときは索引構築・検索の両方をGo側のkagomeで行うと決めてあります。

⚠️ **これは決定であって、現状ではありません（2026-09-12に確認）。**
本番のGoには BM25 も kagome も入っていません（`backend/go.mod` に依存すらありません）。
いま本番が使っているのは、目次からのLLM選択と、`directTitlePages` / `identifierPages` /
`linkPages` の決定的候補、そして失敗時の文字bigram `fallbackPages` だけです。
BM25があるのは評価用Python（`eval/retrieval_eval.py`）の文字bigram実装で、
これはkagome版の代わりにはなりません。着手条件は§6-4（Phase 2）にあります。

---

## 3. 全域的タスクへの対応

Top-k検索型のRAGには、原理的に答えられない質問があります。

「歴代で駆動系のトラブルはどう変遷した？」「うちのサークルの設計思想を要約して」
のような、**全体を見ないと答えられない質問**です。上位5件を取ってくる方式では、
そもそも全体を見ていないので答えようがありません。

対応方法は3つ検討しました。

| 手法 | 評価 |
|---|---|
| **MapReduce** | 正確。114回のAPI呼び出しは1回$1程度で許容範囲。ただしリアルタイムでは遅い |
| **Refine** | **不採用**。114件は長すぎ、順序依存で初期の情報が薄れる。10件程度までの手法 |
| **階層的事前要約** | **採用**。MapReduceの結果をキャッシュする。全域質問が即答になる |

採用した階層的事前要約は、次の構成です。

⚠️ **これも決定であって、現状ではありません（2026-09-12に確認）。** L4の生成・保存・
読込・差分更新はどこにも実装されていません。`build_index.py` が作る `lead` は
本文冒頭の規則ベース抽出で、LLMによる要約ではありません。加えて、下の費用見積もりは
Wiki 114ページ時点のもので、現在の構成へそのまま当てはまりません（M46）。

```
L4-a  ページ要約     114個  （各200字）  = 約2.3万字
L4-b  班別・代別要約   ~15個 （各600字）  = 約0.9万字
L4-c  全体要約         1個   （1500字）
```

合計で約3.5万字（〜3万トークン）です。丸ごとコンテキストに入れても安く済みます。

更新への追随は、`list=recentchanges`と`revid`の比較で変更ページを検出し、
そのページの要約だけを再計算 → 属する班・代の要約 → 全体要約、と差分更新します。

---

## 4. RIG（Retrieval Interleaved Generation）の採否

### 判定：**不採用**

理由は3つあります。

1. **そもそも前提が違います。** RIGの本領は、権威ある統計DBと照合して数値を検証することです。
   本件には照合先の正解DBが存在しません。数値（翼幅・重量・出力）はWiki本文に散在していて、
   それ自体が検索対象であって、検証の基盤ではありません
2. **ツール呼び出しループが実質的に上位互換です。** 生成中に「このページを読む必要がある」と
   判断してツールを呼ぶ挙動は、interleaved retrievalそのものです
3. **実装コストが便益に見合いません**

ただし、**検証可能性という思想は取り込んでいます。** 回答に使ったページは
サーバーが索引から出典カードとして表示し、モデルに出典名やURLを作らせません。
回答本文が資料で裏付けられるかどうかは、§5のFaithfulnessで検査します。

---

## 5. 精度評価アプローチ

### 5-1. 評価は「検索」と「生成」の2層に分離する

これは最初に決めておくべきことでした。

混ぜて測ると、精度が悪かったときに**検索が悪いのか生成が悪いのか切り分けられず、
改善のループが回りません。** 実際、この分離があったおかげで
「正しいページを選んでいるのに記載なしと答える」という失敗を特定できました。

### 5-2. 手法の採否

| アプローチ | 採否 | 理由 |
|---|---|---|
| **BLEU / ROUGE** | ❌ **不採用** | 日本語のn-gram一致は分かち書き依存で不安定。RAG回答は「同じ意味・違う表現」が常態。参照回答を書くコストの割にLLM判定との相関が低い。**測っても改善に繋がらない指標は入れない** |
| **埋め込みベース** | △ **補助のみ** | 「意味は近いが事実が誤り」を検出できない。回帰検知（前回との差分監視）にだけ使う |
| **ルールベース** | ✅ **採用** | 安く決定的。ガードレールとして必須 |
| **LLM-as-a-Judge** | ✅ **主軸** | RAG評価の現実解。1問数円 |
| **人手評価** | ✅ **採用（少量・定期）** | 唯一の真の正解。**LLM判定器のキャリブレーションが主目的** |

### 5-3. 指標セット

**検索の評価（自動・決定的）**

- **Evidence Recall@k（節単位）**
  - ⚠️ ページ単位のRecallでは不十分です。**36,443字ある「駆動・フレーム班」がヒットしても、
    正解の節が取れているとは限りません。** そのためゴールデンデータには
    「どのチャンクが根拠か」まで紐づけています
- **all-evidence recall / nDCG**
  - 複数の資料が必要な質問には、MRR（最初の正解の順位）ではなく
    「必要な根拠を全部取れたか」を見ます
- **Context Precision**
  - 取ってきた文脈のうち、実際に使えた割合です

**生成の評価（LLM-as-a-Judge）**

- **Faithfulness（忠実性）**
  - 回答が検索文脈だけで裏付けられるかです。**本件の最重要指標**にしています
  - 引き継ぎ資料では、「答えられない」より「間違って答える」方がはるかに有害だからです
- **Answer Relevancy**: 質問に答えているか
- **Completeness**: 必要な情報を落としていないか（特に手順型）

**Oracle-context評価（切り分け用）**

正解チャンクを直接渡した状態で、生成だけを評価します。
これで「検索が悪いのか生成が悪いのか」が確定します。

**判定器そのものの検証**

判定器を無条件に信じないことも決めています。

- LLM Judgeの**false-accept率**（誤りを正しいと判定してしまう率）
- 人手評価との**Cohen's κ**（一致度）。κが低ければ、判定器の指標としては使えません

**ルールベース（ガードレール）**

- 出典カードが、索引に実在するページだけで組み立てられているか
- 代（世代）の取り違えがないか
- 回答不能型の質問に、正しく「Wikiに記載がない」と答えられたか

### 5-4. ゴールデンデータセット

**Phase 1では30問**とします（ベースライン測定用の最小構成）。
Phase 2以降で50〜80問に拡張します。

| 種別 | Phase 1 | 例 |
|---|---|---|
| 事実検索型 | 10問 | 「大利根飛行場の予約方法は？」 |
| 手順型 | 7問 | 「トラックの積み込み手順は？」 |
| 比較・横断型 | 7問 | 「40代と41代の空力設計の違いは？」 |
| 全域型 | 3問 | 「歴代の駆動系の課題の変遷は？」 |
| **回答不能型** | 3問 | 「来年度の予算は？」（**ハルシネーション検出用。削らない**） |

各問には「正解チャンクID」を紐づけます（Evidence Recallの算出に必須です）。

### 5-5. 運用

- 検索評価: CIで毎回自動実行します（数秒・無料）
- LLM判定: 変更時に全問実行します（30問×3指標で数百円）
- 人手評価: 月1回20問。**LLM判定との一致率（κ）を測り、判定器自体を検証します**

---

## 6. 精度改善手法

効果の大きい順に並べています。

本件での見立ては、**データ品質がプロンプトより効く**というものです。
これは実際に裏付けが取れました。目次のリード文を直しただけで、
パラメータ数が10倍以上違うモデルの差が消えています（M4）。

### 6-1. データ品質（実装済みのものを含む）

| 施策 | 状態 | 効果 |
|---|---|---|
| **パンくず付与**（ページ名 > H2 > H3） | ✅ 実装済 | ★★★ 検索精度に最も効く |
| **階層マージ**（節191字 → 中央値867字） | ✅ 実装済 | ★★★ 文脈の断絶を防ぐ |
| **最上位見出しをまたぐ結合の抑制** | ✅ 実装済 | ★★ ただし小グループ同士は結合を許す（厳格にすると細切れになる） |
| **用語集の構築**（TF・鳥コン・桁・リブ・プラホ・ペラ） | ✅ `facts.md`へ18語（2026-08-10） | ★★★ **新入生は略語を知らない前提で質問する。** Wikiが「〜とは」で定義している語は実質3語しかないのに、TFは288回・リブは346回出る |
| **代・班のメタデータ付与** | △ 部分的（§6-2） | ★★★ 決定的フィルタの前提 |
| **表のMarkdown変換** | ✅ 実装済（rowspan未対応） | ★★ |
| **画像キャプション抽出** | ✅ 実装済（M21で日本語パラメータの取りこぼしを修正） | ★★ 図の情報が失われるのを防ぐ |
| **リダイレクトを別名として保持** | ✅ 実装済（3件） | ★★ 別名で検索されても引ける |
| **スタブを目次に残し検索から外す** | ✅ 実装済（25件） | ★ 「存在は知らせる、中身は無い」 |
| **カテゴリの保持** | ✅ 実装済 | ★ |
| **`<nowiki>`/`<code>`/`<pre>`の保護** | ✅ 実装済 | ★ 中のマークアップが壊れるのを防ぐ |

### 6-2. 代（世代）の抽出 — 想定より難しい

実測は**タイトル由来26 / 本文由来16 / 複数代32 / 不明40**（114ページ中）です。

タイトルからのルール抽出だけでは23%しか埋まりません。本文スキャンを足して65%です。
根拠の強さが違うので、`gen_source`で区別しています。

> **本文スキャンの正規表現は、長い間まったく機能していませんでした（2026-08-10に修正）。**
> 前後に`\b`を置いていたのですが、Pythonの`\b`は日本語の文字も語構成文字として
> 扱います。そのため「第42代」の前にも「42代の」の後にも境界が立たず、
> **日本語で最も普通の書き方が一つも当たっていませんでした。**
>
> さらに直してみて分かったのは、**単一の代を割り当てること自体が誤りだった**という
> ことです。「駆動・フレーム班」は見出しに「40代引き継ぎ」「41, 42代引き継ぎ」を持つ
> 36,261字のハブページで、代の出現は38代20回 / 40代9回 / 42代9回 / 36代5回。
> 最頻値を採れば38代になりますが、正しくは**36〜42代を扱うページ**です。
>
> そこで「2回以上かつ次点の2倍以上」でなければ代を決めず、`gen_source="mentions"`
> として目次に`36〜42代`と範囲を出すようにしました。決められないときは決めません。

> **ここで重要な気づきがありました。不明65件のすべてが抽出失敗ではありません。**
> 「ESP32のGPIOについて」「トラックの積み込み」のように、
> **そもそも代に依存しない知識のページ**が相当数あります。
>
> したがってLLM補完のタスクは「代を当てる」ではなく、
> **「代依存か、代非依存かを分類し、代依存なら代を推定する」**ことになります。

### 6-3. データローダー

テンプレートが実質ゼロなので、wikitextを直接処理しています。

> ⚠️ v0.1にあった「wikitext直 vs HTML経由のA/B検証」は、**不要と判明したため取り下げました。**

未対応のまま許容しているものが2つあります。表のrowspan/colspanと、
チャンクのオーバーラップです（後者は親子チャンクで代替する想定で、Phase 2で要検証）。

### 6-4. 検索手法（Phase 2）

| 手法 | 採否 |
|---|---|
| **メタデータ事前フィルタ** | △ **部分的**。アシスタントの出所・区分による絞り込みだけ実装済み。質問中の「41代の」「電装班の」を使う質問駆動のフィルタは未実装で、Go側は `categories` / `gen_source` / `headings` / `links` を読み捨てている。`Gen` は本番検索から一度も参照されない |
| **質問中の実在タイトル候補** | ✅ 実装済。型番候補と合わせて最大2件に制限し、LLM用の2枠を残す |
| **BM25**（kagome、Go側に一本化） | △ **未実装**。固有名詞に強く、q17（目次に出しようがない表の1行）を1位で引ける。着手条件は「同じ型の失敗がもう数件」（M28） |
| **ベクトル検索とのハイブリッド** | △ BM25で不足と測れたら |
| **親子チャンク** | △ **未実装**。チャンクに親IDが無く、選んだ節をそのまま回答へ渡している |
| **リランキング** | △ 効果を測ってから |
| **クエリ書き換え・拡張** | △ 用語集ができてから |
| **HyDE** | ❌ 現時点では見送り |

### 6-5. プロンプト品質

- **出典の強制**: 参照したページ名と最終更新日を必ず示します
- **代の明示**: どの代の情報かを明示します。代不明のページはその旨を伝えます
- **不知の許容**: Wikiに記載がなければ推測せず「記載がない」と答えます（**最優先の制約**）
- **鮮度の警告**: 参照元が2年以上前なら、情報が古い可能性を添えます

---

## 7. 決定事項

ここが、この文書で一番読んでほしい部分です。

**表の「決定」は、根拠に反論できない限り変えないでください。** 迷ったときは
「根拠」の列を読めば、なぜそう決めたのかが分かるようにしてあります。

| 論点 | 決定 | 根拠 |
|---|---|---|
| **設置形態** | **Webアプリ。Discordは入り口として追加**（2026-09-13） | WASA全体のDiscordは存在せず、代ごとのDiscordしかない。代を超えて引き継ぐツールが、代ごとに消える場所にあるのは矛盾。**ただしこれは「Discordに置く」ことへの反論であって、「Discordから呼べる」ことへの反論ではない。** 知識の正本は索引とWikiにあり、Discordは入り口にすぎない。代のDiscordが消えても索引もWebアプリも残る。⚠️ 常時接続のボット（Gateway）にはしない。`min-instances=0` が使えず月8,000円規模になり、費用方針と両立しないため、スラッシュコマンド（HTTP Interactions）に限る |
| **アクセス制御** | **Wikiの通常アカウントでログイン**（MediaWiki `action=clientlogin`に中継 → 利用者名を載せた署名付きCookie）。§8-1参照 | 新しいパスワードを配らずに済み、退部者はWiki側で止めれば締め出せる。利用者名ごとにレート制限できる。費用ゼロ |
| **管理者** | **共有アカウントは使わず、`ADMIN_USERS`を画面から外せない主管理者にする。主管理者だけが、ログイン実績のある個人Wiki利用者を共同管理者へ追加・解除できる** | 共有資格情報では本人を監査できず、退部者だけを失効できない。Firestoreだけに主管理者を置くと全員を誤解除した際に復旧できないため、環境設定を復旧口に残す。共同管理者からの権限再付与は禁止し、変更は実名で監査する。全利用者名と回数は表示するが質問本文は出さない |
| **保守の主体** | 当面は開発者本人。API費用も個人負担 | READMEに「壊れたときの直し方」を書く方針は維持 |
| **バックエンド** | **Go** | 公式SDKあり・単一バイナリ・goroutineで並列要約・kagomeで形態素解析が完結・**コールドスタートが速い**（Cloud Runのゼロスケールで実際に効く） |
| **フロントエンド** | **TypeScript + React（Vite / SPA）** | Next.jsはBFF層を内包し、Goでバックエンドを分離する意図と重複する |
| **オフライン処理・評価** | **Python** | 既存の`dump_wiki.py`を活かす。評価の集計はPythonが速い |
| **データストア** | **索引はJSON、利用回数・履歴・共有アシスタント・フィードバック・管理用集計はFirestore** | 現行約3.1MBの索引にDBは使わない。HMAC化した利用者配下へ日次回数（90日）と最大30件の会話を置く。管理画面用には実名・初回/最終利用を1年、本文なし利用ログを90日、管理者監査を1年、Gemini実送信回数を保存する。質問本文は管理画面へ出さず、フィードバックの報告者キーも書き出さない。ブラウザはWiki認証済みGo APIだけを通す |
| **デプロイ** | **フロント= Cloudflare Pages / バック= Cloud Run（asia-northeast1）** | ⚠️ v0.1の「Goバイナリに同梱」と矛盾していたため**分離構成で確定**。CORSはGo側10行程度 |
| **更新反映** | **管理画面から変更を確認し、再構築は`python rebuild.py`、本番反映は人が確認** | Cloud Runが現行索引のWiki改訂番号・公式サイト更新日と公開元を読み取り専用で照合するため、保守者のMacが動いている必要はない。Wikiの専用確認アカウントはSecret Managerへ置く。誤編集の即時反映を避けるため、本文取得・再構築・公開は自動化しない |
| **データの公開範囲** | **コードのみpublic。Wiki本文・インデックスはコミットしない** | 非公開Wikiの内容。`dump/` `data/` `.env`を`.gitignore`。デプロイ時もローカルから直接投入し、GitHubを経由させない |
| **会話の継続** | **直近2往復だけを検索・回答へ渡す** | q33で、現在の質問だけでは正解チャンク342位、直近会話を添えると1位。全履歴を毎回送ると費用が会話長に比例する。過去の回答は誤っている可能性があるため、指示語の解決だけに使い、事実は今回の資料で再確認する |
| **資料外の一般知識** | **汎用・アシスタントとも、見出しで分離した場合だけ許可**（2026-08-09に変更） | 当初はアシスタントを資料範囲へ完全に閉じていたが、それは**アシスタントを汎用より一方的に弱くする**設定で、使う理由を削っていた。設計の質問は実際、資料の設計値と一般的な理論（誘導抗力・材料力学）を組み合わせないと答えにならない。守るべき境界は一般論の可否ではなく、**WASA固有の事実（設計値・手順・連絡先・現在の状態）を資料だけに限ること**である。「一般知識（WASA資料外）」の見出しで分離させ、読み手がどちらの根拠か判別できるようにする。汎用との違いは**参照範囲と口調・形式だけ**になった |

### 費用方針（API以外はゼロ）

学生サークルのツールなので、費用は最初から設計に入れています。

| 項目 | 費用 |
|---|---|
| Cloudflare Pages | ¥0（帯域・リクエスト無制限、`*.pages.dev`でドメイン購入も不要） |
| Cloud Run | ¥0（無料枠が月180,000〜240,000 vCPU秒。月500質問で使用率3%以下） |
| Artifact Registry | ¥0（0.5GBまで無料。Goのイメージは20MB程度） |
| Firestore | 無料枠内を想定（利用者ごとに日次1件＋最大30履歴。運用後に実測する） |
| Claude API | Sonnet 5で約¥3.5/質問、Opus 5で約¥9/質問 |

**請求事故の防止（必須）**

無料枠は、設定を1つ間違えるだけで簡単に超えます。次の6つは必ず守ってください。

1. `min-instances = 0`を維持します（`1`にすると無料枠を確実に超えます）
2. 初期運用は`max-instances = 1`にします（Geminiの送信間隔と停止状態をプロセス内で
   共有しているためです。2台以上にする前に、外部の共有制限が必要になります）
3. 予算アラートを¥1で設定します
4. **Go側に利用者単位のレート制限を入れます**（利用者1人あたり1日30質問程度）。
   HMAC化した利用者キーでFirestoreの回数を原子的に確保し、
   別端末や再起動でリセットされないようにします
5. **Gemini利用時はプロジェクト全体でも制御します。** 呼び出しを直列化して4秒以上空け、
   短時間の429 / 503は最大2回だけ再試行します。短時間上限は1〜15分、
   日次上限は再試行せず太平洋時間0時まで停止し、無駄な再送を防ぎます
6. **Geminiのデータ利用条件を組織で承認してから使います。** 2026-08-08のWASA会議で、
   代表・PMが非公開Wiki本文の無料枠への送信を許可しました。本番は、有料枠なら
   `GEMINI_PAID_TIER=true`、今回承認された無料枠なら`GEMINI_FREE_TIER_APPROVED=true`を
   明示した場合だけ起動します

**API費用の削減**

プロンプトキャッシュ（1時間TTL）と、段階別のモデル選択を使います。

画面ではプロバイダ固有の名前ではなく `auto` / `thinking` の2択だけを見せ、
高速・標準は自動判定内部の段階として扱います。モデルIDはサーバー側の許可リストへ閉じます。

M2b-2で、ページ選択には判断力が必要だが、回答の出典形式は軽量モデルでも31/31で
守れることが分かりました。そこで標準モードはページ・節選択だけを強くし、
回答生成は軽い設定を使っています。

比較は`eval/response_mode_eval.py`で検索段から先に測り、改善が確認できたモデルだけを
環境変数へ設定します。なお、**Claudeのfast modeは単価が2倍のため引き続き不採用**です。

---

## 8. 認証と残る未決事項

### 8-1. 認証：Wikiアカウント連携（決定済み・v0.3で共有パスワードから変更）

ここは一度作り直した箇所なので、経緯も含めて書いておきます。

**当初は共有パスワード1本でした。** 実装は1時間で済んだのですが、
運用に載せる前に致命的な欠点が出ました。

- 誰が使ったか分からない（**監査できない**）
- 退部者を個別に締め出せない（**失効できない**）
- **利用者を区別できず、レート制限や将来の監査を個人単位にできない**

決め手は3つ目でした。費用の安全弁を利用者単位にする必要があり、
そのためには誰が使っているかを識別できなければなりません。

**採用した方式**は、ログインフォームで受け取った利用者名とパスワードを、
バックエンドが対象Wikiの`action=clientlogin`にそのまま中継し、
`status: PASS`なら正規化された利用者名を載せた署名付きCookieを発行する、というものです。

| 論点 | 評価 |
|---|---|
| 新しいパスワードの配布 | **不要**。全員すでにWikiのアカウントを持っている |
| 失効 | **Wiki側でアカウントを止めるだけ**。こちら側の作業ゼロ |
| 個人の識別 | 利用者名が取れる。レート制限の按分に使える |
| 費用 | ¥0。外部IDプロバイダも独自ドメインも要らない |
| Wiki管理者の協力 | **不要**。APIを叩くだけで、設定変更を頼む必要がない |

⚠️ **ただし代償があります。アプリがWikiのパスワードを一度受け取る中継点になります。**

これは軽い話ではないので、譲れない前提を3つ置いています。

1. パスワードは**保存しない・ログに出さない**。検証に使って即座に捨てます（`internal/wiki/auth.go`）
2. Cookieに載せるのは**利用者名と有効期限だけ**。HMAC署名し、HttpOnly / Secureを付けます
3. **HTTPS必須**。フロントを別オリジンに置くため`SameSite=None`になり、Secureが前提になります

あわせて、次も守ります。

- 認証経路は、通常アカウントの`action=clientlogin`だけに統一します
- BotPasswords用の旧`action=login`分岐は持ちません
- 画面には、WASA Wikiの利用者名とパスワードだけを入力させます
- パスワードをアプリへ渡すこと自体の是非を見直す場合は、別の認証方式として改めて検討します

**Cloudflare Accessを採らなかった理由**も書いておきます。独自ドメインが前提で
年1,000〜2,000円かかり、「API以外は¥0」と衝突します。加えて部員のメールアドレス一覧を
別途管理する必要があり、**Wikiのアカウント一覧というすでにある名簿**を
二重管理することになるためです。

### 8-2. その他の未決事項

まだ決まっていない、あるいは確認が済んでいないものです。

1. **画像67枚の扱い** — M21で測定済みです。図が唯一の情報源なのは15箇所あり、
   数式画像とピン配置図が該当します。ただしゴールデンデータの正解チャンクに図は0件のため、
   画像処理の追加は保留しています
2. **答えてはいけない情報の有無** — 個人情報、金銭、非公開の交渉事など。ダンプ内容の確認が必要です
3. **Wiki管理者への一報** — 非公開Wikiの内容を外部（LLM API）へ送信し、
   別の場所で閲覧可能にする以上、共有は済ませておくべきです
4. **MediaWiki 1.37.2はサポート終了版**（2021年12月リリース）です。
   本プロジェクトの対象外ですが、管理者に共有する価値があります
5. **利用場面と頻度** — コスト試算の精度に影響します

---

## 9. 次のステップ

**本番公開と管理画面まで実装済み**です。現在は機能を広げる段階ではなく、
測定値を確定し、実利用の報告を改善へ戻す段階です。

| 優先 | 次に行うこと | 完了条件 |
|---|---|---|
| A | 修正済みの評価器で35問を3反復する | Faithfulnessの範囲と出典欠落の原因が分かる |
| A | LLM Judgeを20問の人手評価で校正する | Cohen's κとfalse-accept率を記録する |
| A | 本番フィードバックの保存・書き出しを確認する | JSONL／CSVへ安全に取り出せる |
| B | 実利用の所要時間を分析する | 段階別p50 / p95を回答モード別に出す |
| B | PC・スマホで本番スモークテストを続ける | ログイン、質問、履歴、評価、管理画面を確認する |

詳細と着手条件は[TODO.md](../TODO.md)、機能追加が無くても残る運用課題は
[09-運用課題.md](09-運用課題.md)を正本とします。**数字で不足が確認されるまでは検索層を増やしません。**

現行の仕様は、責務ごとに文書を分けています。画面は[05-フロントエンド.md](05-フロントエンド.md)、
プロンプトは[03-プロンプト.md](03-プロンプト.md)、
決定的ルールは[02-データ検索とルール.md](02-データ検索とルール.md)、
認証は[04-バックエンド.md](04-バックエンド.md)を正本とします。

---

## 10. レビュー履歴

過去に受けた指摘と、それをどう扱ったかの記録です。

同じ指摘が再び出たときに、「一度検討したうえでこう決めた」と示せるように残しています。

### v0.2（Codexレビュー反映）

**受け入れて修正した指摘**

- `build_index.py`の実バグ: 最上位見出しをまたぐチャンク結合、パンくずが先頭節からしか
  作られない、空行のない長段落が上限を超える、サイズ判定にレンダリング後の長さを
  使っていない、リダイレクト3件の消失、カテゴリの破棄、`<nowiki>`/`<code>`等の未処理、
  画像正規表現がネストリンクを壊す、`revid`不在で差分更新ができない
- **索引（SudachiPy）と検索（kagome）で形態素解析器が食い違うとBM25が壊れる** → kagomeに一本化（§2）
- 除外ページが35%と過大 → スタブ閾値を見直し、目次には残す方式へ（76→89ページ）
- 「階層的要約＝GraphRAGと機能的に等価」は言い過ぎ → 撤回（§1）
- L0〜L4を初期版から全部入れるのは過剰 → フェーズ分けを明記（§2）
- ページ単位Recallでは不十分 → Evidence Recall（節単位）、Oracle-context評価、
  Judgeのκ / false-accept率を追加（§5-3）
- ドキュメント内の矛盾3箇所（デプロイ構成、更新方針、ゴールデンデータ問数）→ 解消（§7）
- 認証方式のトレードオフ、Cloud Runの費用スケール、MediaWiki 1.37のEOL → §7・§8に記載

**反論し、判断を変えなかった指摘**

- **「共有パスワードではなくIAP / Cloudflare Accessを使うべき」**
  — 指摘自体は正しいものでした。ただしCloudflare Accessは独自ドメインが前提で
  年1,000〜2,000円かかり、「API以外は¥0」と衝突します。
  **Wikiアカウント連携という第三の選択肢**で、費用ゼロのまま失効・監査・個人識別を得ました（§8-1）
- **「チャンクにオーバーラップがない」**
  — 親子チャンク（節で検索し親ページを渡す）で代替する想定です。Phase 2の検証項目に留めます
- **グラフRAG不採用の判断そのもの**
  — 表現は弱めましたが、117ページ規模での構築コストは便益を上回るという結論は維持します

### 実測による改善（v0.1 → v0.2）

| | v0.1 | v0.2 |
|---|---|---|
| 検索対象ページ | 76 | **89** |
| チャンク上限超過 | 21件（最大3,414字） | **0件**（最大2,431字） |
| 200字未満チャンク | 122件 | **27件** |
| 画像の取りこぼし | 8箇所 | **0**（57/57） |
| リダイレクト | 消失 | 別名として吸収 |
| 代の抽出 | 21% | **43%**（+ 代非依存ページの存在を認識） |
